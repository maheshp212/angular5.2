import { AddParmasPipe } from './add-parmas.pipe';

describe('AddParmasPipe', () => {
  it('create an instance', () => {
    const pipe = new AddParmasPipe();
    expect(pipe).toBeTruthy();
  });
});
